import type { backendInterface } from "../backend";
import { GradeLevel, SessionStatus, UserRole, UserRole__1 } from "../backend";

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  applyProgressUpdate: async () => ({
    __kind__: "ok",
    ok: {
      xp: BigInt(1200),
      lastPlayedDate: "2026-05-13",
      unlockedAchievements: ["first_quest", "speed_typer"],
      coins: BigInt(350),
      totalPlayTime: BigInt(7200),
      currentLevel: BigInt(3),
      dailyStreak: BigInt(5),
    },
  }),
  assignCallerUserRole: async () => undefined,
  createSession: async () => ({
    __kind__: "ok",
    ok: {
      status: SessionStatus.waiting,
      participants: [],
      createdAt: BigInt(Date.now()),
      hostPrincipal: { _isPrincipal: true, toText: () => "aaaaa-aa" } as any,
      gameName: "Typing Tournament",
      gameState: "{}",
      sessionId: "session-001",
      maxParticipants: BigInt(4),
    },
  }),
  getAllHubProgress: async () => [
    {
      totalMissions: BigInt(10),
      hubId: "mouse-master",
      lastUpdated: BigInt(Date.now()),
      unlocked: true,
      completionPercent: BigInt(60),
      missionsCompleted: BigInt(6),
    },
    {
      totalMissions: BigInt(10),
      hubId: "keyboard-ninja",
      lastUpdated: BigInt(Date.now()),
      unlocked: true,
      completionPercent: BigInt(30),
      missionsCompleted: BigInt(3),
    },
  ],
  getCallerUserProfile: async () => ({
    username: "StarLearner",
    createdAt: BigInt(Date.now()),
    role: UserRole.student,
    gradeLevel: GradeLevel.basic5,
  }),
  getCallerUserRole: async () => UserRole__1.user,
  getHubProgress: async () => ({
    totalMissions: BigInt(10),
    hubId: "mouse-master",
    lastUpdated: BigInt(Date.now()),
    unlocked: true,
    completionPercent: BigInt(60),
    missionsCompleted: BigInt(6),
  }),
  getMyPersonalBest: async () => ({
    timeSpent: BigInt(45),
    achievedAt: BigInt(Date.now()),
    gameId: "cursor-precision",
    score: BigInt(9500),
    accuracy: BigInt(97),
  }),
  getMyProfile: async () => ({
    username: "StarLearner",
    createdAt: BigInt(Date.now()),
    role: UserRole.student,
    gradeLevel: GradeLevel.basic5,
  }),
  getMyProgression: async () => ({
    xp: BigInt(1200),
    lastPlayedDate: "2026-05-13",
    unlockedAchievements: ["first_quest", "speed_typer"],
    coins: BigInt(350),
    totalPlayTime: BigInt(7200),
    currentLevel: BigInt(3),
    dailyStreak: BigInt(5),
  }),
  getMyRecentScores: async () => [
    {
      completedAt: BigInt(Date.now()),
      timeSpent: BigInt(45),
      gameId: "cursor-precision",
      score: BigInt(9500),
      accuracy: BigInt(97),
    },
    {
      completedAt: BigInt(Date.now() - 86400000),
      timeSpent: BigInt(60),
      gameId: "typing-tournament",
      score: BigInt(8200),
      accuracy: BigInt(92),
    },
  ],
  getSession: async () => null,
  getSubjectSummary: async () => ({
    completedHubs: BigInt(3),
    overallPercent: BigInt(45),
    totalHubs: BigInt(13),
    subjectId: "ict",
  }),
  getTopScores: async () => [
    {
      username: "StarLearner",
      rank: BigInt(1),
      achievedAt: BigInt(Date.now()),
      score: BigInt(9500),
      playerPrincipal: { _isPrincipal: true, toText: () => "aaaaa-aa" } as any,
      accuracy: BigInt(97),
    },
    {
      username: "CyberWizard",
      rank: BigInt(2),
      achievedAt: BigInt(Date.now()),
      score: BigInt(8800),
      playerPrincipal: { _isPrincipal: true, toText: () => "bbbbb-bb" } as any,
      accuracy: BigInt(94),
    },
  ],
  getUserProfile: async () => ({
    __kind__: "ok",
    ok: {
      username: "StarLearner",
      createdAt: BigInt(Date.now()),
      role: UserRole.student,
      gradeLevel: GradeLevel.basic5,
    },
  }),
  getUserProgression: async () => ({
    __kind__: "ok",
    ok: {
      xp: BigInt(1200),
      lastPlayedDate: "2026-05-13",
      unlockedAchievements: ["first_quest"],
      coins: BigInt(350),
      totalPlayTime: BigInt(7200),
      currentLevel: BigInt(3),
      dailyStreak: BigInt(5),
    },
  }),
  getWeeklyScores: async () => [],
  isCallerAdmin: async () => false,
  joinSession: async () => ({
    __kind__: "ok",
    ok: {
      status: SessionStatus.waiting,
      participants: [],
      createdAt: BigInt(Date.now()),
      hostPrincipal: { _isPrincipal: true, toText: () => "aaaaa-aa" } as any,
      gameName: "Typing Tournament",
      gameState: "{}",
      sessionId: "session-001",
      maxParticipants: BigInt(4),
    },
  }),
  leaveSession: async () => ({ __kind__: "ok", ok: null }),
  listWaitingSessions: async () => [],
  recordScore: async () => ({ __kind__: "ok", ok: null }),
  registerProfile: async () => ({
    __kind__: "ok",
    ok: {
      username: "StarLearner",
      createdAt: BigInt(Date.now()),
      role: UserRole.student,
      gradeLevel: GradeLevel.basic5,
    },
  }),
  saveCallerUserProfile: async () => undefined,
  unlockAchievement: async () => ({ __kind__: "ok", ok: null }),
  updateGameState: async () => ({ __kind__: "ok", ok: null }),
  updateHubProgress: async () => ({
    __kind__: "ok",
    ok: {
      totalMissions: BigInt(10),
      hubId: "mouse-master",
      lastUpdated: BigInt(Date.now()),
      unlocked: true,
      completionPercent: BigInt(70),
      missionsCompleted: BigInt(7),
    },
  }),
  updateUsername: async () => ({ __kind__: "ok", ok: null }),
};
